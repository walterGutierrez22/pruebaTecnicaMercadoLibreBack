const PARAMETERS_REQUEST = Object.freeze({
    QUERY: "q",
    ID: "id"
});

const AUTHOR = Object.freeze({
    NAME: "Walter",
    LAST_NAME: "Gutierrez"
});

module.exports = {
    PARAMETERS_REQUEST,
    AUTHOR
  };